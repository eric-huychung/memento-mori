import { View, Text } from 'react-native'
import React from 'react'
import Folder from '../(modals)/folder'

const Home = () => {
  return (
    <View>
      <Folder />
    </View>
  )
}

export default Home